info:
  name: Get products
  type: http
  seq: 2

http:
  method: GET
  url: "{{local_base_url}}/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
